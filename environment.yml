name: hf-cuda
channels:
  - conda-forge

dependencies:
  - python=3.11
  - pip
  - cudnn

  # Install everything via pip to avoid dependency issues
  - pip:
    - torch
    - transformers
    - datasets
    - tokenizers
    - accelerate
    - librosa
    - soundfile
    - faster-whisper
    - PySide6
    - tqdm
